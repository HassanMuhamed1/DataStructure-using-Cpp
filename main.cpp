#include <iostream>
#include "LinkedList.h"
using namespace std;
//concat and merge doesn't work
int main() {
    LinkedList l;
    int A[]={10,20,30,40};
    l.create(A, 4);
    l.Display();
    return 0;
}
